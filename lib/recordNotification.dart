import 'package:flutter/material.dart';

class Recordnotification extends StatelessWidget {
  const Recordnotification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
